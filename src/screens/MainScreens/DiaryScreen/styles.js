import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  calendar: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-evenly",
  },
  date: {},
});
