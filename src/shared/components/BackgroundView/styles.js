import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    justifyContent: "center",
    width: "100%",
    height: "105%",
  },
  backGroundImage: {
    position: "absolute",
    bottom: 0,
    right: -100,
  },
  leaves: { top: -100 },
  stawberry: { width: "50%", top: "40%", bottom: "50%", right: 0 },
});
